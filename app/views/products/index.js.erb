// Append new data
$("<%=j render(:partial => 'products', :object => @products) %>")
  .appendTo($(".infinite-table table"));

// Update pagination link
<% if @products.last_page? %>
  $('.pagination').html("That's all, folks!（これで全てです）");
<% else %>
  $('.pagination')
    .html("<%=j link_to_next_page(@products, 'Next Page', :remote => true) %>");
<% end %>